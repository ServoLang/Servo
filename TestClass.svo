class Math {
    function init() {}

    static function pow(a, b) {
        return a ^ b;
    }

    static function mul(a, b) {
        return a * b;
    }

}

var math = Math();
print(Math().pow(2, math.mul(2, 2)) * 4 / 3 + 0.5);